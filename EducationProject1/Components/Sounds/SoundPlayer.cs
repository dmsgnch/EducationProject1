using System.IO;
using System.Media;
using System.Windows;
using System.Windows.Media;

namespace EducationProject1.Components.Sounds;

public static class SoundPlayer
{
    public static void PlaySoundByPath(string filePath)
    {
        // MediaPlayer Sound1 = new MediaPlayer();
        // Uri uri = new Uri(@"\Resources\Sounds\beep.wav", UriKind.Absolute);
        // Sound1.Open(uri);
        // Sound1.Play();
    }
}